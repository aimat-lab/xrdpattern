[tox]
envlist = pkg, req
toxworkdir=/tmp/.tox/{env:TOX_ENVNAME}


[testenv:pkg]
deps =
    -e ..
commands =
    python -m unittest discover -s . -p "t_*.py"

[testenv:req]
deps =
    -r../requirements.txt
commands =
    python -m unittest discover -s . -p "t_*.py"
